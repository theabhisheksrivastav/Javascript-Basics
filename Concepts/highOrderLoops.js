// for of

const arr = [1, 2, 3, 4, 5];

for (let i of arr) {
    console.log(i);
    }

// forEach

arr.forEach((i) => {
    console.log(i);
    }
);

// map

arr.map((i) => {
    console.log(i);
    }
);

// filter

arr.filter((i) => {
    console.log(i);
    }
);

// reduce

arr.reduce((acc, i) => {
    console.log(i);
    }
);

// find

arr.find((i) => {
    console.log(i);
    }
);

// findIndex

arr.findIndex((i) => {
    console.log(i);
    }
);

// some

arr.some((i) => {
    console.log(i);
    }
);

// every

arr.every((i) => {
    console.log(i);
    }
);

// flat

arr.flat((i) => {
    console.log(i);
    }
);

// flatMap

arr.flatMap((i) => {
    console.log(i);
    }
);

// keys

arr.keys((i) => {
    console.log(i);
    }
);

// values

arr.values((i) => {
    console.log(i);
    }
);

// entries

arr.entries((i) => {
    console.log(i);
    }
);

// from

Array.from(arr, (i) => {
    console.log(i);
    }
);

